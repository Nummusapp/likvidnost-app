export default function HomePage() {
  return (
    <main className="container">
      <h1>Likvidnost app</h1>
      <p>Next.js (App Router) + TypeScript + Prisma + PostgreSQL je pripravljen.</p>
      <p>
        Health endpoint: <code>/api/health</code>
      </p>
    </main>
  );
}
